{
  "name": "@dialob/dialob-parent",
  "version": "2.2.8",
  "private": true,
  "scripts": {
    "ci:publish": "pnpm publish -r --no-git-checks --access=public && pnpm changeset tag",
    "ci:version": "pnpm changeset version && pnpm install --lockfile-only"
  },
  "dependencies": {
    "@types/node": "^22.14.1"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "eslint"
    ]
  }
}
